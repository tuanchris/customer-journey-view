version: 2

sources:
  - name: ecommerce
    tables:
      - name: distribution_centers
        columns:
          - name: id
            tests: ['unique', 'not_null']
      - name: inventory_items
        columns:
          - name: id
            tests: ['unique', 'not_null']
      - name: order_items
        columns:
          - name: id
            tests: ['unique', 'not_null']
      - name: products
        columns:
          - name: id
            tests: ['unique', 'not_null']
      - name: users
        columns:
          - name: id
            tests: ['unique', 'not_null']
      - name: events
        columns:
          - name: id
            tests: ['unique', 'not_null']
