version: 2

sources:
  - name: ecommerce
    tables:
      - name: orders
        columns:
          - name: id
            tests: ['unique', 'not_null']
      - name: order_items
        columns:
          - name: id
            tests: ['unique', 'not_null']
      - name: products
        columns:
          - name: id
            tests: ['unique', 'not_null']
      - name: customers
        columns:
          - name: id
            tests: ['unique', 'not_null']
      - name: events
        columns:
          - name: id
            tests: ['unique', 'not_null']
